#include <iostream>
#include <vector>
#include <string>
 
 using namespace std;
 struct t
 {
 	int i;
 	int score;
 	int count;
 	vector<char> ans;
 }
 
 int main()
 {
 	int n,m;
 	cin >> n >> m;
 	vector<t> ts(m);
 	for (int i=0;i<m;i++)
 	{
 		ts[i].id=i;
 		cin >> ts[i]score >> ts[i].count;
 		int count=0;
 		cin>>count;
 		vector<char> answers(count);
 		ts[i].ans=answers;
 		for (int j=0;j<cout;j++)
 		{
 			cin >> ts[i].ans[j];
		}
	}
	getchar();
	for (int i=0;j<n;j++)
	{
		string input;
		getline(cin,input);
		for (int j=0;j<input.length();j++)
		{
			int a = input.find("(");
			int 
		} 
	}
 }
